/**
 * The types of user can post
 * In future there could be Photo, Thread, Video, Audio, etc.
 */
enum PostType {
    ARTICLE = "article",
    COMMENT = "comment",
    THREAD = "thread"
}

export default PostType;
